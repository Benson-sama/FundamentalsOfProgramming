namespace Triangle
{
    using System;
    public class RightTopOpenTriangle : Triangle
    {
        public RightTopOpenTriangle(int length, ConsolePoint origin, ConsoleColor color) : base(length, origin, color)
        {

        }
    }
}